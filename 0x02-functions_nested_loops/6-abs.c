#include "main.h"

/**
  * _abs - Entry point
  * @t: is an integer
  * Description: prints the absolute value of an integer
  * Return: the absolute value
  */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
